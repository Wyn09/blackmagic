[project]
name = "blackmagic"
version = "0.1.0"
description = "Final Fantasy XIV Black Mage Simulator"
requires-python = ">=3.8"
dependencies = [
    "pygame>=2.5.0",
]

[project.optional-dependencies]
dev = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
